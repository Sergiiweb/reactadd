import {useReducer} from "react";
import './App.css';

const initialState = {

}
const reducer = (state, action) => {
    switch (action.type) {

    }
}

function App() {
    useReducer(reducer, initialState);

    return (
        <div>
            App
        </div>
    );
}

export default App;
